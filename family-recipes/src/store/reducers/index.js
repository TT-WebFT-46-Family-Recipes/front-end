

const InitialState = {
    users: [],
    recipes: [],
    isLoading: false,
    error: ''
}

export const reducer = (state = InitialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}